# Primary voice transcription uses the same-origin /api/transcribe endpoint.
# Optional realtime mode:
# VITE_CALLS_SIGNALING_URL=http://localhost:8787/api/calls/offer
# VITE_CALLS_ICE_SERVERS_URL=http://localhost:8787/api/calls/ice-servers
