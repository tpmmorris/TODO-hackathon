{
  "name": "@gpnow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:init": "wrangler d1 execute gpnow-db --local --file=schema.sql"
  },
  "dependencies": {
    "@gpnow/types": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "wrangler": "^4.13.2"
  }
}
